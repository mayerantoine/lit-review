# OpenAI API Configuration
# Required for embeddings, relevance scoring, and text generation
OPENAI_API_KEY=your_openai_api_key_here

# Deployment Environment Configuration
# Determines the default ChromaDB persist directory
# Options: "local" (default) | "azure"
# - "local": Uses ./corpus-data/chroma_db
# - "azure": Uses /data/chromadb (for Azure Files mount)
DEPLOYMENT_ENV=local

# ChromaDB Persist Directory Override (Optional)
# If set, this overrides the auto-detected persist directory
# Useful for custom deployment configurations
# CHROMA_PERSIST_DIR=/custom/path/to/chromadb

# Azure-specific configuration (when DEPLOYMENT_ENV=azure)
# When deploying to Azure App Service with Azure Files:
# 1. Set DEPLOYMENT_ENV=azure
# 2. Mount Azure Files to /data/chromadb in the container
# 3. The application will automatically use /data/chromadb for persistence
