# OpenAI API Configuration
# Required for embeddings, relevance scoring, and text generation
OPENAI_API_KEY=your_openai_api_key_here

# =============================================================================
# Deployment Environment
# =============================================================================
# Options: "local" (default) | "azure"
# Controls the default ChromaDB persist directory:
#   - "local": uses ./corpus-data/chroma_db
#   - "azure": uses /data/chromadb (for Azure Files mount)
DEPLOYMENT_ENV=local

# =============================================================================
# Vector Storage Configuration
# =============================================================================
# Storage mode for ChromaDB vectors
# Options: "persistent" (default) | "in_memory"
#   - "persistent": stores index on disk (survives restarts)
#   - "in_memory": stores index in RAM only (lost on restart, no disk I/O)
VECTOR_STORAGE_MODE=persistent

# ChromaDB persist directory (optional override)
# If set, this overrides the DEPLOYMENT_ENV-based default above.
# Examples:
#   VECTOR_PERSIST_DIRECTORY=./corpus-data/chroma_db   (local default)
#   VECTOR_PERSIST_DIRECTORY=/data/chromadb             (Azure default)
# VECTOR_PERSIST_DIRECTORY=./corpus-data/chroma_db

# =============================================================================
# Azure-specific deployment notes
# =============================================================================
# When deploying to Azure App Service with Azure Files for ChromaDB persistence:
#   1. Set DEPLOYMENT_ENV=azure   (auto-sets path to /data/chromadb)
#   2. Mount Azure Files share to /data/chromadb in the container
#   3. Set VECTOR_STORAGE_MODE=persistent
#   The application will automatically persist the vector index across restarts.
